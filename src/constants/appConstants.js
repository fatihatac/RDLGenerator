const EXCLUDED_KEYS = ["TarihAralik"];

const VIEW_MODES = {
  DESIGN: "design",
  PREVIEW: "preview",
  XML: "xml", 
};

const ITEM_TYPES = {
  TITLE: "title",
  TABLE: "table",
  DATA: "data",
  DATE_RANGE: "dateRange",
  CHART: "chart",
  TEXTBOX: "textbox",
  RECTANGLE: "rectangle",
};

export { EXCLUDED_KEYS, VIEW_MODES, ITEM_TYPES };
